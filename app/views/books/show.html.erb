<%= form_with model: @book do |f| %>

  <h4>Title:</h4>
  <span><% @book.title %></span>
  
  <h4>Body:</h4>
  <span><% @book.body %></span>

  <%= link_to 'Edit', edit_book_path(book.id) %>｜<%= link_to 'Back', "/books" %>

<% end %>